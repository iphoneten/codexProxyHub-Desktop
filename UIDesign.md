# recodexProxyHub UI/UX 优化方案与规范说明

**版本：** v1.0 | **适用端：** Rust 桌面客户端网关后台 | **核心目标：** 去冗余、强交互、标准化视觉层级

---

## 1. 概述与核心优化原则

当前 `recodexProxyHub` 的界面框架已经具备优秀的后台控制台雏形（采用“侧边栏导航 + 主内容工作区”模式）。然而，为了提升其作为一款专业开发者/网络工程代理工具的生产力属性，需要解决当前存在的**信息高频冗余**、**核心状态反馈模糊**以及**操作组件不直观**等问题。

**核心重构原则：**
* **高内聚，低冗余：** 侧边栏回归全局导航本质，杜绝详细配置列表与主内容区的二次重复铺陈。
* **直观交互反馈：** 将传统多选框升级为语义更明确的开关组件，让网络通道的开启/关闭状态一目了然。
* **提效降本：** 针对 Base URL 等高频复制项，引入快速工具链（如一键复制），缩短核心操作路径。

---

## 2. 页面结构与信息流优化

### 2.1 侧边栏重构
原设计在侧边栏下方放置了完整的“渠道列表”，而主内容区又包含了一个功能完全重合的“渠道概览”表格。这种设计会严重分散视线并造成认知过载。
* **修改方案：** 彻底移除侧边栏下方的独立“渠道列表”区域。
* **调整后逻辑：** 侧边栏仅保留“概览”、“渠道”、“鉴权”、“路由”四个一级标准导航标签。若用户需要查看或修改全部渠道，应直接点击“渠道”导航项。

### 2.2 主工作区卡片化
当前主内容区的模块（服务配置、渠道概览）视觉上直接平铺在白色背景上，层级较扁平。建议将主背景改为浅灰蓝色（`#f8fafc`），并将各个功能块封装进独立的卡片。

* **旧版缺陷分析：**
    1. 模块边界不清晰，表格与输入框紧凑排列。
    2. 顶部四个数据块缺乏背景框包裹，容易与下方内容混淆。
    3. “未运行”状态提示条与全局操作按钮缺乏强关联。
* **新版优化策略：**
    1. 引入带边框的卡片（`border: 1px solid #e2e8f0`），增加轻微阴影。
    2. 每个模块卡片保持固定的内边距。
    3. 使用清晰的视觉留白分隔服务配置与表格。

---

## 3. 关键组件与交互升级规范

| 优化模块 | 当前设计 | 推荐修改方案 | 交互提升点 |
| :--- | :--- | :--- | :--- |
| **渠道启用状态** | 使用复选框（Checkbox）<br>`☑ 已启用` `☐ 停用` | 全面替换为 **Toggle Switch (状态开关)**。<br>启用：绿色激活态；停用：灰色背景。 | 变“选择行为”为“开关状态”，符合路由/代理状态切换的主流行业直觉。 |
| **Base URL 区域** | 纯文本显示：<br>`http://127.0.0.1:8001/v1` | 放入灰色禁用输入框内，右侧内置 **复制图标按钮**。 | 免去开发者手动用鼠标划选、容易错选漏选的痛苦。 |
| **控制按钮组** | “启动代理”与“停止”两按钮并排长存。 | 改为**互斥状态按钮**。未运行时仅显示蓝色“启动代理”；运行后变为红色的“停止代理”。 | 避免在“未运行”时误触“停止”产生的逻辑歧义。 |
| **Host/Port 输入** | 裸露的单线 HTML 输入框。 | 包裹进标准输入框，在 Host 前加网络图标，Port 前加插头图标。 | 增强桌面客户端的端侧原生质感。 |

---

## 4. 视觉层级与数据看板精细化

### 4.1 顶部数据看板升级
* **信息层级倒置：** 将标签文本（如“模型数量”）颜色变浅变小；将数据主体（如 “30”）加粗并显著放大。
* **引入图标辅助：** 在卡片内部增加微小的彩色图标（如网络图标对应渠道，安全锁图标对应鉴权）。

### 4.2 数据表格美化
* **名称加粗与强调：** 表格内的“名称”（如 anyrouter）应加粗，方便快速扫视。
* **配置数据标签化：** 将优先级（P1, W2）和模型数量转化为带背景色的圆角小标签（Badge）。
* **表格对齐规范：** 状态、名称等左对齐；模型数量等数值列右对齐或居中对齐。

---

## 5. 视觉色彩规范定义

建议在前端渲染层统一配置以下设计变量：
* **品牌主色 (Primary):** `#2563eb` (科技蓝，用于主按钮、导航激活态)
* **成功/运行状态 (Success):** `#10b981` (翡翠绿，用于运行中状态 Tag、已开启的 Switch)
* **警告/停止状态 (Danger):** `#ef4444` (珊瑚红，用于停止代理按钮、异常状态提示)
* **背景色 (Background):** `#f8fafc` (浅滑石灰，作为主窗体底色)
* **边框线 (Border):** `#e2e8f0` (极淡灰，用于卡片与表格边框)

---

## 6. 字体排版与文字大小规范 (Typography Scale)

为了保证桌面端阅读的舒适度和信息层级的清晰，推荐采用以下字体排版层级（以标准的 `14px` 为基准正文大小）：

| 层级 (Level) | 适用场景 (Usage) | 字号 (Size) | 字重 (Weight) | 颜色 (Color) |
| :--- | :--- | :--- | :--- | :--- |
| **大数字 (Stat/Display)** | 顶部数据看板的核心数值（如模型数、渠道数） | **28px** | 粗体 (700) | `#0f172a` (极深黑) |
| **主标题 (Heading 1)** | 页面顶部核心大标题（如应用名称） | **24px** | 粗体 (700) | `#0f172a` (极深黑) |
| **模块标题 (Heading 2)** | 各功能区大标题（如“服务”、“渠道概览”） | **18px** | 半粗体 (600) | `#1e293b` (深黑) |
| **正文主体 (Body)** | 表格数据、侧边栏导航、输入框内文本 | **14px** | 常规 (400/500) | `#334155` (主体灰黑) |
| **辅助文本 (Caption)** | 表头 (Th)、配置说明提示、徽章(Badge)内的文字 | **12px** | 常规 (400) | `#64748b` (次级灰) |

**排版细节建议：**
* **字体家族 (Font Family)：** 推荐优先使用系统原生无衬线字体，如 `system-ui, -apple-system, "Segoe UI", Roboto, sans-serif`，以获得最佳的跨平台渲染效果。
* **行高 (Line Height)：** 正文行高建议设为 `1.5` 或 `1.6`；标题和组件内部行高可紧凑些，设为 `1.2` 或 `1.25`。